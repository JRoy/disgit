name = "disgit"
main = "src/index.ts"
compatibility_date = "2025-01-01"

workers_dev = true

[vars]
IGNORED_BRANCH_REGEX=""
IGNORED_BRANCHES=""
IGNORED_USERS=""
IGNORED_PAYLOADS=""
DEBUG_PASTE=false

# Uses secrets:
# - GITHUB_WEBHOOK_SECRET
# Run `echo <VALUE> | wrangler secret put <NAME>` for each of these
